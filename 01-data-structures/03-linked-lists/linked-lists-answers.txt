1. Spatial locality means that arrays have elements in them stored side by side, which is better to go through a list of elements.

2.Linked Lists take longer to add and remove elements than pointing to just one element.
